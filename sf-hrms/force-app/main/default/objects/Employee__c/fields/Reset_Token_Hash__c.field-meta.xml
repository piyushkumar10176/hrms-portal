<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Reset_Token_Hash__c</fullName>
    <label>Reset Token Hash</label>
    <type>Text</type>
    <length>128</length>
    <description>SHA-256 of the password reset token. The token itself is only ever in the email; storing the hash means a database read cannot be replayed as a reset link.</description>
</CustomField>
