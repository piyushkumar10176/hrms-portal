<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <customSettingsType>Hierarchy</customSettingsType>
    <description>Slack gateway configuration. A protected custom setting rather than custom metadata on purpose: the signing secret is data, so it is set in this org and never travels in source control.</description>
    <enableFeeds>false</enableFeeds>
    <label>Slack Config</label>
    <visibility>Protected</visibility>
</CustomObject>
